# BETS LEDGER (owner: @edge)

Your track record is how the captain prices your next call. One row per bet. No row, no trust.

| Date | Bet | Cost | Outcome | Lesson |
|------|-----|------|---------|--------|
| 2026-09-22 | ROUTER-V2 (pre-registered, `runs/2026-09-22-router-round-02/missions/PRE-REGISTRATION.md` sha256 7e14c9d2…): routed ladder ≥ 20/20, grave=0, cost ≤ 88, routing ≥ 16/20 on seed 7; hold-rule seed-stable; rule-stack byte-deterministic | 0 (sandbox) | LANDED-SPLIT (2026-09-22): 20/20 grave=0 held; hold-rule seed-stable + byte-deterministic held; cost leg LOST (127 > 88); routing leg LOST (0/20, semantics flaw per ADR-10); resolved won 9 vs 4 | Falsifier fired on cost+routing: champion stays `solo --hold-on-vague`; class table on R03 trial |
| 2026-09-22 | ROUTER-V2 R03 (pre-registered, `runs/2026-09-22-router-round-03/missions/PRE-REGISTRATION-R03.md` sha256 1abb2853…): ADR-10 semantics reproduces solo 11/20; class-table arm reproduces R02 on the frozen 20; real ablation runs the ladder; **H13 predicts KILL of the class table** on the 46-task fixture (cost premium, no resolved gain); expanded arms seed-stable + byte-deterministic | 0 (sandbox) | **LANDED** (2026-09-22): solo 11/20 ✓ (frozen 20, pre-cut revision); table arm reproduced R02 exactly (20/20, grave 0, cost 127, resolved 9) ✓; ablation ran the ladder (hops 25/55) ✓; **H13 = KILL** — 46/46 both arms, resolved 23 vs 23 (clause c FAIL), 307 vs 292 calls (clause d FAIL) ✓; sd=0.000 across 5 seeds and double-runs byte-identical for both ladder arms ✓; post-cut verification ALL-PASS | The registered prediction landed on every leg, which is why the round's value is in the *ruling*, not the prediction: the table's only distinguishable win was a diagnostic metric (44/46 vs 25/46 agreement) that bought zero resolved deliveries. Next: judge the ladder's +11 resolved against a **resolved-per-cost** bar registered in advance (R04), and price the verifier tax with a `--no-verifier` ablation. |
| 2026-09-22 | ROUTER-V2 R04 (pre-registered, `runs/2026-09-22-router-round-04/missions/PRE-REGISTRATION-R04.md` sha256 ad9451ce…): the verifier tax is exactly 2 calls per verified task (census: 7 of 46 tasks fire → 14 calls); **X2 predicts the gate has a hole** — `unverified_deliveries(pre) > 0` because the verifier runs before the ladder and the gate is one-way DELIVER→HOLD; X3 predicts `verify=final` closes it at no extra cost; X5 predicts the ruling is REPLACE pre with final | 0 (sandbox) | **LANDED-SPLIT** (2026-09-22), with one leg MISSED: exact tax ✓ (14 calls of 292 = 4.79%, per-task identity `cost(pre) − cost(off) == verifier_calls` on all 46 tasks, reproduced independently through the literal `--no-verifier` alias); a hole exists ✓ (but **one** task, T23, not the aggregate 19 — the other 18 were never selected by the trigger); `final` cheaper and closes it ✓ (288 vs 292, holes 1 → 0, quality identical); **directional leg LOST** ✗ — the ruling was KEEP `pre`, because the registered clause `unverified_deliveries(final) == 0` measured 18 and could never have held. Decision: the stage **stays** (tax too small to justify deleting the only independent check, and the instrument cannot price its benefit); no widen; `final` deferred to R05 with a corrected `hole_deliveries` clause | A clause built on a conflated metric fails for reasons that have nothing to do with its mechanism: the aggregate mixed "trigger never selected it" with "selected but returned unguarded" (18 vs 1), and direction can only be bet after the metric is decomposed. Corollary for the ledger itself: this is the first R04-style miss recorded against @edge, and it is recorded as a miss, not re-scored. Next: R05 registers the corrected clause plus the trigger-composition trial (18 of 19 unselected deliveries) and the owed resolved-per-cost bar. |
| 2026-09-22 | ROUTER-V2 R05 (pre-registered, `runs/2026-09-22-router-round-05/missions/PRE-REGISTRATION-R05.md` sha256 2f5c4b46…): on a fresh 40-task fixture with declared `wound` labels — **P1** (pre-GREEN, from the fixture + plan rules alone) `union` selects 9 tasks (3 wound), `form` 7 (3), `risk` 2 (0), `all` 40 (16); **P2** (pre-GREEN, from the fixture + the registered signal definitions) the `wound` trigger selects 18 tasks — 14 declared wound plus the 4 clean traps — so `recall ≤ 0.875`, `precision ≤ 0.778`; **P3** (post-GREEN) `all` reaches recall 1.0 at ~66 calls and `wound` beats it on `wound_per_call`, winning Rule B → **REPLACE `union` with `wound`**; **P4** (post-GREEN) Rule A fires → **REPLACE `pre` with `final`**; **P5** (post-GREEN) quality invariant across all six arms (40/40, 0 graves, 33 resolved) | 0 (sandbox) | PENDING | — |

**Disclosure on the R05 row (before its outcome exists):** unlike R02–R04, R05's
pre-registration was frozen after RED *and* after the implementation existed, so
the GREEN suite had already printed arm summaries on screen when P3–P5 were
written. P1 and P2 were derivable from the fixture and the registered rules with
no arm and no gate, and they are marked pre-GREEN. The rules, arms, endpoints
and fixture were fixed before any arm ran; no rule was changed after seeing a
number. This disclosure is repeated in the pre-registration's own header.
