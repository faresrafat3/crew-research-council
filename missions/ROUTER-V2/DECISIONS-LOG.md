# DECISIONS-LOG — mission ROUTER-V2 (append-only)

| Date (UTC) | Decision | Why | Alternatives rejected | Reversal condition |
|---|---|---|---|---|
| 2026-09-22 | Sandbox copied from `2026-09-21-router-round-01` (not from repo trees) | Round-01 sandbox is the newest runnable revision; repo trees are protected | Building from `crew-research-council/{crew,harness}` (protected; only `__pycache__`) | Newer runnable revision appears |
| 2026-09-22 | RED suites written before any implementation; 43 failed / 1 passed witnessed | Iron Law (TEST-POLICY §3); the plan's H1–H9 need falsifiable tests first | Implement-then-test | None — law, not preference |
| 2026-09-22 | Frozen round-01 scorer moved verbatim to `crew/router_thresholds.py` | Legacy tests and the ablation arm need the untouched baseline; keeps `router.py` v2 clean | Rewriting the scorer | If ablation becomes unnecessary after H5 ruling |
| 2026-09-22 | `used=ROUTED` label in eval output instead of per-task table formation | The ladder decides per task; the honest label is the mode, not the table's first guess | Printing table formation as `used` (would fake routing_match) | Round-03 routing_match redefinition may print both |
| 2026-09-22 | T17 guard test passes task constraints to scan_source (test updated, mechanism unchanged) | The ban is task-relative by definition; guard reads the brief's contract, never rewrites it | Hard-coding banned phrases in the guard | A measured poison class that is task-independent |
| 2026-09-22 | propose_priors min_failures=1 (was 2 in first implementation) | Proposals are free and advisory; the gate is the apply lock. One measured failure is evidence enough to propose | Keeping 2 (would miss single-failure classes) | If proposal spam appears, raise and gate harder |
| 2026-09-22 | Debate verdict computed from receipt counts, not turn count | The loudness-wins failure (DECISIVE-01 arm B) must be structurally impossible | LLM-style free debate | Model leg (R05) may revisit with judged turns |
| 2026-09-22 | routed NOT promoted despite H1 compliance win (cost 127 > 88 bar; H5 refuted) | Promotion rule (pre-registration §Promotion) requires cost bar AND table value; both failed | Promoting on quality alone | Round-03: table trial or cut; cost ≤ 88 then re-rule |
| 2026-09-22 | Measurements run only after bets-ledger row was appended | The bet must be on the record before the outcome exists (no hindsight bets) | Writing the bet after results | None — law |
| 2026-09-22 (R03) | Sandbox `2026-09-22-router-round-03` copied from round-02; frozen fixture sha re-verified identical (`2840667…`) | R03 must inherit R02's revision exactly, so the trial's continuity claims are checkable | Rebuilding from the protected trees | Newer runnable revision appears |
| 2026-09-22 (R03) | Expanded fixture authored as 46 tasks = frozen 20 verbatim + 26 new, all 17 classes | H5 needs power beyond n=20 (pre-registration §Frozen inputs); keeping the 20 first makes continuity provable by equality | A wholly new 40-task fixture (breaks continuity comparisons) | If a class proves unrepresentative, add instances in R04 and re-pin |
| 2026-09-22 (R03) | Expanded fixture validated for champion solvability (46/46, grave 0) BEFORE the freeze, and declared as fixture evidence | A fixture that the champion cannot solve would make every arm's failure uninformative | Freezing an unvalidated fixture | None — validation precedes use |
| 2026-09-22 (R03) | Declared budget 2000 calls for every arm; the harness governor is now applied to ladder arms too | The budget is per run, not per task; the expanded fixture needs 159 calls for the champion. R02 let ladder arms bypass it — an unfair cost comparison | Keeping the default 100 (fires mid-run) or leaving the bypass | A class needs more than the cap: raise per class on the record |
| 2026-09-22 (R03) | H16 registered as a *limitation measurement* (`verifier_flips`) rather than assuming the verifier's value | The blind reviewer is the same rule set that grades the run, so detection value is structurally unmeasurable here; the honest move is to measure that and say so | Claiming the verifier prevented graves (unfalsifiable on this fixture) | A judge independent of the grader exists (model leg, R05) |
| 2026-09-22 (R03) | Class table DELETED after H13 KILL (clauses c and d FAIL: 23 vs 23 resolved; 307 vs 292 calls) | Pre-committed consequence in `PRE-REGISTRATION-R03.md`; unused mechanism = standing debt (P7) | Keeping it behind a flag; promoting it on its 44/46 diagnostic win | A ledger-proposed class prior survives its own pre-registered trial on new fixtures |
| 2026-09-22 (R03) | `route()` lost the `use_table` switch instead of keeping it as a legacy no-op | With one proposal source there is nothing to switch; a dead flag is the same debt the razor just cut | Keeping `use_table` for old callers | Never — callers were updated in the same commit-set |
| 2026-09-22 (R03) | Two tests re-based after the ruling: `test_routed_matches_used_on_fixture` (R02 proxy) → `test_router_has_single_proposal_mechanism`, and the two R03 continuity tests | The ruling deleted the mechanism they pinned. Their *values* were not dropped: the pre-cut numbers are asserted against the frozen trial log (`test_trial_log_holds_the_precut_semantics_evidence`), and the new assertions are strictly stronger (no table exists at all) | Editing them to pass silently; deleting the pre-cut evidence | None — the artifact holds the old numbers permanently |
| 2026-09-22 (R03) | Pre-cut revision pinned by sha256 (`logs/precut-sha256.txt`) before the cut was applied | A ruling that changes the code must leave the measured revision identifiable, or its evidence cannot be audited | Relying on git history (sandbox is outside version control) | None |
| 2026-09-22 (R03) | The ≤88-call promotion bar is explicitly NOT rewritten after the data | Changing a bar post hoc is post-hoc rationalization; the honest move is to register a resolved-per-cost bar for R04 on the record | Lowering the bar to promote the ladder now | R04 pre-registers the new bar and measures against it |
| 2026-09-22 (R03) | Board appends (BLACKBOARD.md, `missions/ROUTER-V2/BOARD.md`) executed as full-file writes and then **verified byte-identical** against pre-write copies (`head -120` / `head -104` diff) | The patch-style edit tool was unavailable in this session, and an unverified rewrite of frozen board prose would violate P14; the diff makes the append provably additive | Appending unverified; or writing the R03 block into a detached sibling file | If a diff ever shows a changed frozen line, restore from the pre-write copy and redo |
| 2026-09-22 (R03) | R03 papers mirrored into `missions/ROUTER-V2/` as new dated files (`*-R03.md`), while R02's `FINDINGS.md`, `BOARD.md`, `PRE-REGISTRATION.md` and `ADR-ROUTER-V2.md` stay frozen; only `PRINCIPLES.md` and `DECISIONS-LOG.md` are superseded in place (their R03 versions are strict supersets) | The mission directory is the canonical paper surface, but rewriting a frozen round's findings would destroy its record; doctrine and the append-only log are different: they are meant to accumulate | Overwriting R02's FINDINGS/ADR in place; leaving R03 papers only in the sandbox | If a later round needs to amend an R02 paper, add another dated file beside it |
| 2026-09-22 (R04) | Sandbox `2026-09-22-router-round-04` copied from round-03 (post-cut revision); both fixture shas re-verified identical (`2840667…` / `26b3b83…`); baseline witness `111 passed` recorded before any R04 edit | R04 must inherit the exact revision R03 ruled on, or its prices and continuity claims are unanchored | Rebuilding from the protected trees; copying round-02 (pre-cut, would resurrect the deleted table) | Newer runnable revision appears |
| 2026-09-22 (R04) | The verification stage gets one explicit mode flag (`--verify pre|final|off`, with `--no-verifier` as an alias for `off`) instead of ad-hoc ablation switches | The Owner's ablation is a first-class configuration, not a test hack: the alias must select the *same* code path as `off`, and the mode must be visible in every SUMMARY block and ledger row | A separate `--skip-verify` boolean that bypassed only part of the stage (alias drift risk); leaving the ablation to a hidden env var | A mode is added or renamed — then the suite's alias-identity test is updated with the round that changed it |
| 2026-09-22 (R04) | Registered before measuring: the decision rule may reach CUT only through `tax_pre == 0`, and `off` may never be chosen | The grader *is* the rule set the verifier re-runs, so `verifier_flips = 0` is instrument blindness; deleting a safety mechanism on an instrument that cannot price its benefit is a value claim with no evidence (new P15) | A rule that would cut the stage on zero observed catches; a rule that could choose `off` when cheaper | An evaluator independent of the rule set prices detection value (model leg, R05 gate) |
| 2026-09-22 (R04) | `--no-verifier` ablation run on the 46-task fixture and the frozen 20, in the pre-registered form; the stage **stays** | Measured exactly: 14 calls of 292 (4.79%) on 46 tasks, 4 of 122 (3.28%) on 20; per-task identity `cost(pre) − cost(off) == verifier_calls` holds on all 46; outcomes identical (passed 46/46, grave 0, resolved 23) in every mode. The price is bounded, attributable and charged only where the trigger fires | Trimming or deleting the stage ("it caught nothing") — forbidden by the no-CUT clause; widening the trigger to all 46 deliveries — arithmetic projection 2 × 46 = 92 calls (+33% over the `off` arm's 278) for the same unmeasured benefit; hidden, so R05 owns the trigger trial | A rejection that causes a repair hop (the price would then include the hop), or an independent evaluator pricing detection |
| 2026-09-22 (R04) | `verify=final` (retarget to the returned artifact) implemented, measured, and **NOT adopted**; carried to R05 as a *deferred trim* with a corrected clause | It weakly dominates the shipped mode on every measured axis (tax 10 < 14; hole 1 → 0; guarded 5 > 4; quality identical) but fails the clause frozen before the data (`unverified_deliveries(final) == 0`, measured 18). Adopting it now would be a post-hoc edit of a frozen rule on the very data that revealed the misspecification (new P18) | Shipping it inside R04 ("it is strictly better"); deleting the mode and losing the measurement; quietly re-scoring the clause after the fact | R05 pre-registers a `hole_deliveries`-based clause on a fixture not used to define the metric and it holds — then `final` becomes the default and ADR-14 is superseded |
| 2026-09-22 (R04) | **Deviation (post-measurement, exploratory):** the coverage metric was split into `hole_deliveries` (selected + unguarded) and `unselected_deliveries` (trigger never fired) after X3 was refuted on the aggregate | The aggregate mixed two causes: 18 of the 19 unguarded deliveries were never selected, so the registered clause could never have held and the hypothesis was refuted for reasons unrelated to its mechanism (new P16). The split is marked exploratory, is *not* used in the ruling, and the original aggregate is still reported for continuity with the R04 pre-registration | Ruling on the ambiguous metric; replacing `unverified_deliveries` outright (would break the pre-registered metric's reproducibility) | A coverage question is provably captured by one number — then collapse the split with the reason on the record |
| 2026-09-22 (R04) | X3's refutation is asserted in the suite rather than deleted from it (`test_verifier_targeting_r04.py`) | A refuted registered claim is evidence; deleting the failing assertion would erase the record of what R04 predicted and what the data said (P14, P2) | Removing the claim after it failed; rewording it to pass | Never |
| 2026-09-22 (R04) | The T23 post-hop hole is recorded as a defect with its measured fix price (+1 call on 46 tasks, +0 on 20) and deliberately **not** fixed this round | The fix is one extra guard pass inside `pre`, i.e. a partial retarget; shipping it after seeing the data bends the frozen rule and makes R05's `pre` vs `final` comparison uninterpretable. Practical cost of deferring is nil: zero rejections were observed, so the hole has never transmitted a wrong delivery (new P17, ADR-16) | Fixing it immediately with a one-line patch ("it is a real bug"); leaving it unmentioned in the papers | A rejection is ever observed on an artifact that reached delivery through a hop — then it ships immediately as a correctness patch, outside the trial protocol, citing that event |
| 2026-09-22 (R04) | The champion's asymmetry is reported in the generated champion row: `solo --hold-on-vague` runs **no** verification mechanism (12 of 12 deliveries unverified on the 46 fixture) | The 14-call tax is the *ladder's*, not the system's; any "the ladder costs more" claim that omits the champion's zero-verification stance is a misreading. Reported, not hidden | Presenting a single tax number without the arm's baseline | `solo` gains a risk-gated check and is priced (R05 future work 3) |
| 2026-09-22 (R04) | Variance sweep (seeds 7/11/13/17/19) and byte-identical double runs run for **all three** modes, not only the shipped one | A mode's determinism claim is a property of the mode; the variant that may become the default must carry the same evidence as the incumbent (P9) | Sweeping only `pre` and inheriting its verdict for `final`/`off` | Never |
| 2026-09-22 (R04) | An independent reproduction of the tax via the literal `--no-verifier` alias (`tools/ablation_no_verifier_r04.py` → `logs/r04-no-verifier-ablation.txt`), separate from the trial's `--verify off` arm | The headline number must be reproducible through the exact interface the Owner named, not only through the internal mode name; an alias that drifts would otherwise go unnoticed | Relying on the trial's `off` arm alone as the ablation evidence | The alias is removed or renamed |
| 2026-09-22 (R04) | R04 papers mirrored as dated files (`FINDINGS-R04.md`, `ADR-ROUTER-V2-R04.md`, `REPRO-PACK-R04.md`) plus a fresh `logs/r04-no-verifier-ablation.txt`; `PRINCIPLES.md` (P15–P18) and `DECISIONS-LOG.md` superseded in place | Same rule as R03: frozen rounds' papers are never rewritten, while doctrine and the append-only log are meant to accumulate. The frozen region of `PRINCIPLES.md` was verified byte-identical (`head -159` diff against the inherited R03 copy) before the append was accepted | Overwriting R03's papers; leaving R04's doctrine only in the sandbox | A later round needs to amend an R03 paper — then add another dated file beside it |
| 2026-09-22 (R04) | Nothing promoted; the ≤88-call promotion bar stands untouched, and the owed resolved-per-cost bar is registered for R05 rather than set now | The bar cannot be chosen by the round that wants to pass it; R04 touched verification timing, not routing, and the ladder still costs 122 on the frozen fixture | Lowering the bar after the ablation showed the tax is small; promoting on the ladder's 23 resolved | R05 pre-registers a resolved-per-cost bar before measuring |
